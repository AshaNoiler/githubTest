#include <stdio.h>

double multiply(double *num1, double *num2) {
  // double num1, num2;
  // printf("i will multiply these 2 numbers: ");
  // scanf("%lf %lf", &num1, &num2);
  printf("the product is %lf\n", *num1 * *num2);
  return 0.0;
}